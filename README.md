# 🦍 Ape Authority Associates

> Bold Republican-themed satirical clothing brand.

## Quick Start

```bash
# Clone and open locally
git clone https://github.com/YOUR_USERNAME/ape-authority-site.git
cd ape-authority-site

# Open in browser (no build needed - it's static!)
open index.html
```

## Deployment

This site auto-deploys via **Cloudflare Pages**:

1. Push to `main` branch
2. Cloudflare detects the change
3. Site goes live in ~1 minute

**Live URL**: `https://apeauthorityassociates.com` (after DNS setup)

## Tech Stack

- **HTML5** - Semantic structure
- **CSS3** - Custom properties, animations, glassmorphism
- **Vanilla JS** - No frameworks, lightweight
- **Printify** - Print-on-demand fulfillment
- **Cloudflare Pages** - Free hosting with auto-deploy

## Git Commands Reference

```bash
# Make changes, then:
git add .
git commit -m "Your message"
git push

# That's it - Cloudflare handles the rest!
```

## File Structure

```
apeAuthority-site/
├── index.html      # Main page
├── styles.css      # All styling
├── script.js       # Interactions
├── README.md       # This file
└── .gitignore      # Git ignores
```

---

*Built with 🦍 energy and Git pushes*
